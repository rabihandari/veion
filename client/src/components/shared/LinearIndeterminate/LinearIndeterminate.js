import React from 'react';
import { CustomIndeterminate } from './styles';

const LinearIndeterminate = ({ visible }) => {
    return(
        <CustomIndeterminate  />
    );
}

export default LinearIndeterminate;