import { makeStyles } from '@material-ui/core'

export default makeStyles((theme) => ({
    button: {
        borderRadius: '50px',
        borderColor: '#000000C4',
        borderWidth: '8px',
        borderStyle: 'solid',
        padding: '15px 30px',
        fontWeight: '700',
    },
}))